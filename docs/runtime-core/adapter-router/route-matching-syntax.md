# 路由的匹配语法

使用的是 `react-router-dom` 的路由匹配语法，它 **不支持任何正则表达式语法** 和 **可重复的参数语法**，写法只停留在 `:param*` 与 `*` 这两种通配符模式。

由于在路由匹配语法这方面，它们之间的底层设计不相同，从而没必要进行强行中间层面的语法转换嫁接，这会增加许多不必要的复杂度，也可能带来意料之外的错误。因此，你只需使用 react-router-dom 提供的合法写法即可。